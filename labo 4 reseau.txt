- su - root (switch entre user lambda et user root)
- setxkbmap be (switch le clavier en be)
- ip addr show (affichage cartes réseaux + config réseau)
- ip link show (affichage infos niveau 2 ex: mac address)
- ip route show (affichage des routes configurées)
- cat (affichage contenu fichier texte)
- cat /etc/resolv.conf (affichage contenu fichier resolv.conf)
- apt-get install resolv.conf (installer resolv.conf au cas ou il n'est pas installé)
- nano (modifier fichier texte)
- nano /etc/network/interfaces (modifier le fichier interfaces contenant toutes les données des interfaces)
- systemctl restart networking (redémarrage du syst§me réseau pour qu'il se mette à jour si fichier de configuration réseau modifié)
- ifdown "interface" (déconfiguration interface)
- ifup "interface" (configuration interface)
- ifquery "interface" (affichage liste paramètres liés à l'interface)
- apt-get install ifupdown (installer ifupdown si pas installé de base)
- hostname (afficher le hostname)
- nano /etc/hostname (changer le fichier contenant les hostnames)
- hostnamectl set-hostname "hostname" (changer le hostname en une commande)
- ip addr flush deb "interface" (retirer config interface)
- ip addr add 192.168.10.20/24 dev "interface" (ajouter une adresse supplémentaire)
- ip addr del 192.168.10.20/24 dev "interface" (supprimer une adresse)
- ip addr flush dev "interface" ( configurer une interface en dhcp, d'abord supprimer adresse statique)
- ip route del default (suppression route par défaut)
- ip route add default via 192.168.10.40 (ajout route par défaut via l'adresse)
- ip route add 192.168.1.0/24 via 192.168.10.40 (ajout route statique via l'adresse)
- ip route del 192.168.1.0/24 (suppression route statique)
- ip link set "interface" down (mettre une interface down)
- ip link set "interface" up (mettre une interface up)
- ip link set dev "interface" address aa:bb:cc:aa:bb:cc (changer adresse mac)